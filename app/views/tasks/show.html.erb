<div class="container text-center">

<h2><%= @task.name %></h2>
<p><%= @task.details %></p>

<%= link_to "Edit", edit_task_path(@task) %>
<br>
<%= link_to "Delete", task_path(@task), method: :delete, data: { confirm: "Are you sure ?" } %>
<br>
<%= link_to "Back to ToDoList ?", tasks_path %>
</div>
